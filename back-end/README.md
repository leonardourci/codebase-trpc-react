# Codebase

## Environment Configuration

Create a `.env` file based on `.env.example` with your database connection string.

The application automatically uses the test database (Docker on port 5433) when `NODE_ENV=test`.
